const CustomerProps = ['primaryCustomerSuccessManager', 'legalName', 'legalForm', 'vTigerAccountNumber', 'primarySbiCode', 'googleString'];

const PersonProps = ['initials', 'givenName', 'namePrefix', 'surname'];

module.exports = {
  CustomerProps,
  PersonProps
};
